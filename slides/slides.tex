\documentclass{beamer}
% \usepackage{showframe}

\usepackage[american]{babel}
\usepackage[T1]{fontenc}

\usepackage{fontspec}
\setsansfont{Jost}

\usepackage{emoji}
\setemojifont{Twemoji Mozilla}

\usepackage{microtype}
\usepackage{csquotes}

\usepackage{xcolor}
\usepackage{hyperref}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{mathtools}
\usepackage{stmaryrd}
\usepackage{wasysym}

\usepackage{tikz}
\usetikzlibrary{calc}
\usetikzlibrary{backgrounds}
\usetikzlibrary{tikzmark}
\usetikzlibrary{decorations.pathreplacing}
\usetikzlibrary{positioning}
\usetikzlibrary{arrows.meta}
\usetikzlibrary{overlay-beamer-styles}

\usepackage{booktabs}
\usepackage{xparse}

\input{defs.tex}

% \includeonlyframes{goals}

\title{Relational Separation Logic for Compiler Verification}

\author{
  \texorpdfstring%
  {Gabriel Desfrene\\Supervised by Xavier Leroy and François Pottier}
  {Gabriel Desfrene}
}

\date{September 2026}

\begin{document}

\begin{frame}[label=title]
  \titlepage
\end{frame}

\begin{frame}[label=goals]
  \frametitle{Research Goals}

  \begin{center}
    \begin{tikzpicture}[
        box/.style={
          align=center,
          font=\small,
          draw=blueDraw,
          fill=blueFill,
          text=blackText,
          rounded corners=6pt,
          thick,
          inner sep=6pt
        },
        main-box/.style={
          align=center,
          font=\Large,
          draw=grayDraw,
          fill=grayFill,
          text=blackText,
          rounded corners=6pt,
          thick,
          inner sep=8pt
        },
        chk/.style={
          font=\small,
          inner sep=.5pt,
          fill=white,
          rounded corners=1pt,
          xshift=-.4ex,
          yshift=-.4ex,
        },
        arr/.style={
          ->, thick, draw=grayDraw,
          shorten >=2pt,
          shorten <=2pt,
        }
      ]
      \bbox{}

      \coordinate (l1) at ($(topmid)!0.1!(bottommid)$);
      \coordinate (l2) at ($(topmid)!0.5!(bottommid)$);
      \coordinate (l3) at ($(topmid)!0.9!(bottommid)$);

      \coordinate (c1) at ($(topleft)!0.025!(topright)$);
      \coordinate (c2) at ($(topleft)!0.5!(topright)$);
      \coordinate (c3) at ($(topleft)!0.975!(topright)$);

      \node[main-box] (triple) at (l2 -| c2) {$\{P\}\; F(c) \lesssim c \;\{Q\}$};

      \node<2->[box, anchor=north, text width=3cm]
      (n1) at (l1 -| c2) {Express compiler correctness?};

      \draw<2->[arr] (n1) -- (triple);

      \node<3->[box, anchor=west, text width=1.7cm]
      (n2) at (l2 -| c1) {Invariants $P, Q$?};

      \draw<3->[arr] (n2) -- (triple);

      \node<4->[box, anchor=south, text width=3cm]
      (n3) at (l3 -| c2) {Which reasoning\\rules?};

      \draw<4->[arr] (n3) -- (triple);

      \node<5->[box, anchor=east, text width=1.7cm]
      (n4) at (l2 -| c3) {Separation Logic?};

      \draw<5->[arr] (n4) -- (triple);

      \node<6->[chk] at (n1.north east) {\emoji{check-mark-button}};
      \node<6->[chk] at (n2.north east) {\emoji{check-mark-button}};
      \node<6->[chk] at (n3.north east) {\emoji{check-mark-button}};
      \node<6->[chk] at (n4.north east) {\emoji{check-mark-button}};
    \end{tikzpicture}
  \end{center}
\end{frame}

\section{Context}

\begin{frame}[plain, noframenumbering, label=intro-ctx]
  \vfill
  \centering
  \huge
  \textcolor{title.fg}{Context}
  \vfill
\end{frame}

\begin{frame}[label=bugs]
  \frametitle{Compilers Can Introduce Bugs}

  \begin{center}
    \begin{tikzpicture}[
        compiler/.style={
          draw=grayDraw,
          fill=grayFill,
          text=blackText,
          minimum height=1cm,
          minimum width=2.5cm,
          rounded corners,
          thick
        },
        comp/.style={
          align=center, text width=3cm, font=\small
        },
        arr/.style={
          ->, thick,
          shorten >=2pt,
          shorten <=2pt,
          draw=blackDraw
        }
      ]
      \bbox{}

      \node<2->[compiler]
      (compiler) at ($(topmid)!0.25!(bottommid)$) {Compiler};

      \node<2->[left=of compiler, text=colSrc] (source) {Source Program};
      \node<2->[right=of compiler, text=colTgt] (target) {Target Program};

      \draw<2->[arr] (source) -- (compiler);
      \draw<2->[arr]
      (compiler) -- node[above]
      {
        \begin{tikzpicture}[baseline]
          \node<3->[inner sep=0pt] (bug) at (0, 0) {\emoji{bug}};
          \only<4->{%
            \fill[white, fill opacity=0.6] (bug.south west) rectangle (bug.north east);
          }
        \end{tikzpicture}
      }
      (target);

      \node<4->[below=0.2cm of compiler, text=greenText]
      (verified) {\textbf{Verified}};

      \node<5->[comp] (compcert) at ($(topmid)!0.75!(bottommid)$)
      {CompCert\\ C $\to$ assembly};

      \node<5->[left=1em of compcert, comp]
      (cakeml) {CakeML\\ ML $\to$ machine};

      \node<5->[right=1em of compcert, comp]
      (jasmin) {Jasmin\\ crypto $\to$ machine};
    \end{tikzpicture}
  \end{center}
\end{frame}


\begin{frame}[label=refinement]
  \frametitle{Behavioral Refinement}

  \begin{center}
    \begin{tikzpicture}[
        progbox/.style={
          align=center,
          minimum width=10mm,
          minimum height=6mm,
          background text=grayDraw,
        },
        src box/.style={
          progbox,
          background default text=colSrc,
        },
        tgt box/.style={
          progbox,
          background default text=colTgt,
        },
        center box/.style={
          progbox,
          background default text=blackText,
        },
        beh box/.style={
          align=center, text width=6cm, font=\small, text=grayText
        },
        example/.style={
          text width=45mm,
          align=center,
          font=\scriptsize,
          background default text=blackText,
          background text=grayDraw,
        },
        beh def/.style={
          rounded corners=8pt, fill=grayFill, draw=grayDraw, thick
        }
      ]
      \bbox{}

      \node<2->[beh box] (beh) at ($(topmid)!0.1!(bottommid)$)
      {
        $b \in \left\{
          \texttt{Term}(v, m) \mid \texttt{Div} \mid \text{\lightning}
        \right\}$
      };

      \coordinate<3->[xshift=-10mm] (arrup) at ($(topmid)!0.2!(bottommid)$);
      \coordinate<3->[xshift=-10mm] (arrdown) at ($(topmid)!0.6!(bottommid)$);

      \coordinate<3-> (arrmid) at ($(arrup)!0.5!(arrdown)$);

      \draw<3->[gridline] (arrmid -| topleft) -- (arrmid -| topright);
      \draw<3->[gridline] (arrup) -- (arrdown);

      \node<3->[center box, font=\small, text on=<5->]
      (c2) at ($(arrup -| topleft)!0.5!(arrmid)$) {$\iff$};
      \node<3->[left=1mm of c2, tgt box, font=\small, text on=<5->] {$b \in T$};
      \node<3->[right=1mm of c2, src box, font=\small, text on=<5->] {$b \in S$};

      \node<4->[example, text on=<5->] at ($(arrup)!0.5!(arrmid -| topright)$)
      {$
        \begin{matrix}
          \mathtt{f}
          \big((\mathtt{print}(\mathtt{"A"})); 1),
          (\mathtt{print}(\mathtt{"B"}); 2)\big) \\
          \downarrow \\
          \mathtt{print}(\mathtt{"A"}); \mathtt{print}(\mathtt{"B"}); \mathtt{f}(1, 2)
        \end{matrix}
        $};

      \node<5->[center box, font=\small, text on=<7->]
      (c3) at ($(arrmid -| topleft)!0.5!(arrdown)$) {$\implies$};
      \node<5->[left=1mm of c3, tgt box, font=\small, text on=<7->] {$b \in T$};
      \node<5->[right=1mm of c3, src box, font=\small, text on=<7->] {$b \in S$};

      \node<6->[example, text on=<7->] at ($(arrmid)!0.5!(arrdown -| topright)$)
      {
        $
        \begin{aligned}
          x &\coloneq y / y & &\to & x &\coloneq 1
        \end{aligned}
        $
      };

      \coordinate<7-> (midup) at ($(topmid)!0.7!(bottommid)$);
      \coordinate<7-> (midlow) at ($(topmid)!1!(bottommid)$);

      \def\halfwidth{45mm}
      \coordinate<7-> (topleft) at ($ (midup.north) + (-\halfwidth, 0) $);

      \draw<7->[beh def]
      (topleft) rectangle ($ (midlow.south) + (\halfwidth, 0) $);

      \node<7->[below right=1ex of topleft] (legend) {$\refines{T}{S} \triangleq {}$};

      \node<8->[src box, text on=<1>] at (legend -| topmid) (srcUB)
      {$\text{\lightning} \in S$};

      \node<8->[center box, text on=<1>] (c4) at ($(midup)!0.8!(midlow)$)
      {$\implies$};

      \node<8->[center box, text on=<1>] at ($(srcUB)!0.5!(c4)$)
      {or};

      \node<8->[left=1em of c4, tgt box, text on=<1>] {$b \in T$};
      \node<8->[right=1em of c4, src box, text on=<1>] {$b \in S$};
    \end{tikzpicture}
  \end{center}
\end{frame}


\begin{frame}[label=simulation]
  \frametitle{Simulation Relation}

  \begin{center}
    \begin{tikzpicture}[
        title/.style={
          font=\bfseries,
          % draw
        },
        step arrow/.style={
          ->, thick, draw
        },
        rel/.style={
          dashed, draw=grayDraw
        },
        conf/.style={
          align=center, text width=5mm, minimum height=6mm,
          % draw,
        },
        arr/.style={
          ->, thick, draw=grayDraw
        },
      ]

      \bbox{}

      \node<1> at ($(topmid)!0.45!(bottommid)$)
      {$\begin{aligned}
        \textcolor{colTgt}{t_0 \to t_1 \to {}}
        & \textcolor{colTgt}{\dots \to \mathtt{Term}(v, m)} \\
        & \Downarrow \\
        \textcolor{colSrc}{s_0 \to s_1 \to {}}
        & \textcolor{colSrc}{\dots \to \mathtt{Term}(v, m)} \\
        \end{aligned}
        $};

      \coordinate (relup) at ($(topmid)!0.3!(bottommid)$);
      \coordinate (reldown) at ($(topmid)!0.6!(bottommid)$);

      \node<2->[xshift=-15mm, conf, colSrc] (s1) at (relup) {$s$};
      \node<2->[xshift=15mm, conf, colTgt] (t1) at (relup) {$t$};

      \node<2->[conf, colSrc] (s2) at (reldown -| s1) {$s'$};
      \node<2->[conf, colTgt] (t2) at (reldown -| t1) {$t'$};

      \draw<2->[step arrow, colTgt] (t1) -- (t2);
      \draw<2->[step arrow, colSrc] (s1) -- (s2);

      \draw<2->[rel] (s1) -- (t1);
      \draw<2->[rel] (s2) -- (t2);


      \coordinate<3-> (relmid) at ($(relup)!0.5!(reldown)$);

      \coordinate<3-> (midt) at (relmid -| t2);
      \coordinate<3-> (mids) at (relmid -| s2);

      \node<3->[anchor=west, colTgt] at (midt) {$\forall$};
      \draw<4->[arr] ($(mids)!0.8!(midt)$) to[bend right=25] ($(mids)!0.2!(midt)$);
      \node<5->[anchor=east, colSrc] at (mids) {$\exists$};


      \node<6->[title] at ($(topmid)!0.15!(bottommid)$) {Backward Simulation};
      \node<6-> (arrback) at ($(topmid)!0.7!(bottommid)$) {$\Downarrow$};
      \node<6-> (refback) at ($(topmid)!0.8!(bottommid)$) {$\refines{T}{S}$};
    \end{tikzpicture}
  \end{center}
\end{frame}


\begin{frame}[label=stuttering]
  \frametitle{The Stuttering Problem}

  \begin{center}
    \begin{tikzpicture}[
        step arrow/.style={
          ->, thick, draw
        },
        rel/.style={
          dashed, draw=grayDraw
        },
        conf/.style={
          align=center, text width=5mm, minimum height=6mm,
          % draw,
        },
        arr/.style={
          ->, thick, draw=grayDraw
        },
        fuel/.style={
          text=grayText, font=\small
        },
      ]

      \bbox{}

      \node[conf, colSrc] (s0) at ($(topmid) + (-10mm, -10mm)$) {$s_0$};
      \node[below=10mm of s0, conf, colSrc] (s1) {};
      \node[below=10mm of s1, conf, colSrc] (s) {\alt<5->{$s_1$}{$s$}};

      \node[conf, colTgt] (t0) at ($(topmid) + (10mm, -10mm)$) {$t_0$};
      \node[below=10mm of t0, conf, colTgt] (t1) {\only<2->{$t_1$}};
      \node[below=10mm of t1, conf, colTgt] (t) {$t$};

      % Frame 1
      \draw<1->[step arrow, colSrc] (s0) -- (s);
      \draw<1->[rel] (s0) -- (t0);
      \draw<1->[rel] (s) -- (t);

      \draw<1>[step arrow, colTgt] (t0) -- (t);

      % Frame 2
      \draw<2->[step arrow, dashed, colTgt] (t0) -- node (stut) {} (t1);
      \draw<2->[step arrow, colTgt] (t1) --  (t);

      % Frame 3
      \draw<3-4>[rel] (s1) -- node[above, grayText] (qmark) {?} (t1);

      % Frame 4
      \node<4> (stutlbl) at (stut.east -| {$(topleft)!0.85!(topright)$}) {Stuttering};
      \draw<4>[grayDraw, ->] (stutlbl) -- (stut.east);

      % Frame 5
      \node<5->[below=5mm of s, conf, colSrc] (s3) {$\dots$};
      \node<5->[below=5mm of s3, conf, colSrc] (sinf)
      {\alt<7->{$s$}{$\dots$}};

      \draw<5->[step arrow, colSrc, dashed] (s) -- (s3);
      \draw<5->[step arrow, colSrc, dashed] (s3) -- (sinf);

      % Frame 6
      \node<6->[below=5mm of t, conf, colTgt] (t3) {};
      \node<6->[fuel] (f1) at ($(s0)!0.5!(t1)$) {$1$};
      \node<6->[fuel] (f2) at ($(s1)!0.5!(t)$) {$0$};
      \node<6->[fuel] (le1) at ($(f1)!0.5!(f2)$) {$\vee$};

      \node<6->[fuel] (f3) at ($(s)!0.5!(t3)$) {$n$};
      \node<6->[fuel] (f4) at ($(t3)!0.5!(sinf)$) {$n-1$};
      \node<6->[fuel] (le2) at ($(f3)!0.5!(f4)$) {$\vee$};

      % Frame 7
      \node<7->[below=5mm of t3, conf, colTgt] (tinf) {$t$};
      \draw<7->[thick, double equal sign distance, colTgt] (t) -- (tinf);

      \draw<7->[rel] (sinf) -- (tinf);
    \end{tikzpicture}
  \end{center}
\end{frame}


\begin{frame}[label=limitations]
  \frametitle{Limitations of Existing Approaches}

  \begin{center}
    \begin{tikzpicture}[
        header/.style={
          text=blueText
        },
        side/.style={
          font=\bfseries\small, text=grayText
        },
        elem-bad/.style={
          font=\small, text=redText,
        },
        elem-warn/.style={
          font=\small, text=orangeText,
        },
        missing/.style={
          font=\bfseries\small,
          minimum height=1cm, text width=30mm, align=center,
          fill=greenFill, draw=greenDraw, text=greenText,
          thick, rounded corners=6pt, inner sep=5pt,
        },
      ]
      \bbox{}

      \coordinate (tblstart) at ($(topleft)!0.2!(bottomleft)$);
      \coordinate (tblend) at ($(topleft)!0.85!(bottomleft)$);

      \coordinate (line1) at ($(tblstart)!0.15!(tblend)$);
      \coordinate (line2) at ($(line1)!0.5!(tblend)$);

      \coordinate (col1) at ({$(topleft)!0.22!(topright)$} |- tblstart);
      \coordinate (col2) at ({$(topleft)!0.61!(topright)$} |- tblstart);


      \draw<2->[gridline] (col1) -- (col1 |- tblend);
      \draw<2->[gridline] (col2) -- (col2 |- tblend);

      \draw<2->[gridline] (line1) -- (line1 -| topright);
      \draw<2->[gridline] (line2) -- (line2 -| topright);

      \node<2->[header] (nd) at ($(col1)!0.5!(col2 |- line1)$) {Non-Determinism};

      \node<3->[side] (cakeml) at ($(line1)!0.5!(line2 -| col1)$) {CakeML};
      \node<3->[elem-bad] at (cakeml -| nd) {Not Supported};

      \node<4->[side] (compcert) at ($(line2)!0.5!(tblend -| col1)$) {CompCert};
      \node<4->[elem-warn] at (compcert -| nd) {External Only};

      \node<5->[missing] at (line2 -| nd) {Backward Simulation};

      \node<6->[header, minimum width=36mm] (mem) at ($(col2)!0.5!(topright |- line1)$)
      {Memory Reasoning};

      \node<7->[elem-warn] at (cakeml -| mem) {Global Invariants};
      \node<7->[elem-warn] at (compcert -| mem) {Global Invariants};

      \node<8->[missing] at (line2 -| mem) {Separation Logic};

    \end{tikzpicture}
  \end{center}
\end{frame}

\begin{frame}[label=contributions]
  \frametitle{Contributions}

  \begin{tikzpicture}[remember picture, overlay]
    \bbox[b]

    \coordinate (sep) at ($(topmid)!0.6!(bottommid)$);
    \coordinate (sepleft) at (topleft |- sep);
    \coordinate (sepright) at (bottomright |- sep);

    \coordinate (l2) at ($(sep) - (0, \gaplength + \cellheight/2)$);
    \coordinate (l3) at ($(sep) + (0, \gaplength + \cellheight/2)$);

    \coordinate (l1) at ($(l2) - (0, \cellheight + \gaplength)$);
    \coordinate (l4) at ($(l3) + (0, \cellheight + \gaplength)$);

    \node<2->[box, node-green, minimum width=\cellwidth] at (l1)
    {
      \textbf{Backward Simulation} \\
      {
        \scriptsize\itshape%
        inspired by ``Stuttering For Free''
      }
    };

    \node<3->[box, node-teal, minimum width=\cellwidth] at (l2)
    {
      \textbf{Relational Separation Logic} \\
      {
        \scriptsize\itshape%
        inspired by ``Simuliris''
      }
    };

    \draw<4->[divider] (sepleft) -- (sepright);
    \node<4->[anchor=north west, scope-tag] at (sepleft) {Language\\Independent};
    \node<4->[anchor=south west, scope-tag] at (sepleft) {Language\\Dependent};

    \node<5->[box, node-blue, minimum width=\cellwidth] (rtl) at (l3)
    {
      \textbf{Low-level language} \\
      {
        \scriptsize\itshape%
        inspired by ``CompCert''
      }
    };

    \node<6->[box, node-orange, anchor=west,
    minimum width={\cellwidth*\cellsplit - \gaplength/2}] at (l4 -| rtl.west)
    {
      \textbf{Transformations} \\
      {
        \scriptsize\itshape%
        of concrete programs
      }
    };
 
    \node<7->[box, node-violet, anchor=east,
    minimum width={\cellwidth*(1-\cellsplit) - \gaplength/2}] at (l4 -| rtl.east)
    {
      \textbf{Optimizations} \\
      {
        \scriptsize\itshape%
        of generic programs
      }
    };
  \end{tikzpicture}
\end{frame}

\section{Simulation}

\begin{frame}[plain, noframenumbering, label=intro-sim]

  \begin{tikzpicture}[remember picture, overlay]
    \bbox[b]

    \coordinate (sep) at ($(topmid)!0.6!(bottommid)$);
    \coordinate (sepleft) at (topleft |- sep);
    \coordinate (sepright) at (bottomright |- sep);

    \coordinate (l2) at ($(sep) - (0, \gaplength + \cellheight/2)$);
    \coordinate (l3) at ($(sep) + (0, \gaplength + \cellheight/2)$);

    \coordinate (l1) at ($(l2) - (0, \cellheight + \gaplength)$);
    \coordinate (l4) at ($(l3) + (0, \cellheight + \gaplength)$);

    \node[box, node-green, minimum width=\cellwidth] at (l1)
    {
      \textbf{Backward Simulation} \\
      {
        \scriptsize\itshape%
        inspired by ``Stuttering For Free''
      }
    };

    \node[box, node-gray, minimum width=\cellwidth] at (l2)
    {
      \textbf{Relational Separation Logic} \\
      {
        \scriptsize\itshape%
        inspired by ``Simuliris''
      }
    };

    \draw[divider] (sepleft) -- (sepright);
    \node[anchor=north west, scope-tag] at (sepleft) {Language\\Independent};
    \node[anchor=south west, scope-tag] at (sepleft) {Language\\Dependent};

    \node[box, node-gray, minimum width=\cellwidth] (rtl) at (l3)
    {
      \textbf{Low-level language} \\
      {
        \scriptsize\itshape%
        inspired by ``CompCert''
      }
    };

    \node[box, node-gray, anchor=west,
    minimum width={\cellwidth*\cellsplit - \gaplength/2}] at (l4 -| rtl.west)
    {
      \textbf{Transformations} \\
      {
        \scriptsize\itshape%
        of concrete programs
      }
    };
 
    \node[box, node-gray, anchor=east,
    minimum width={\cellwidth*(1-\cellsplit) - \gaplength/2}] at (l4 -| rtl.east)
    {
      \textbf{Optimizations} \\
      {
        \scriptsize\itshape%
        of generic programs
      }
    };
  \end{tikzpicture}
\end{frame}

\begin{frame}[label=free]
  \frametitle{The Free Simulation}

  \begin{center}
    \begin{tikzpicture}[
        arr/.style={
          ->, thick, background aspect=densely dash dot,
        },
        step arrow/.style={
          arr, background default aspect=solid,
        },
        stut arrow/.style={
          arr, background default aspect=dashed,
        },
        rel/.style={
          dashed, draw=grayDraw
        },
        conf/.style={
          align=center, text width=5mm, minimum height=6mm,
          % draw,
        },
        legend/.style={
          font=\scriptsize,
          draw=none, fill=white, text=grayText,
        },
        legend arr/.style={
          ->, thick, draw=grayDraw,
        },
        fuel/.style={
          text=grayText, font=\footnotesize,
          % draw
        },
        idx legend/.style={
          legend, text width=8mm, align=center
        }
      ]

      \bbox{}

      \coordinate (step0) at ($(topmid)!0.1!(bottommid)$);
      \coordinate (step4) at ($(topmid)!0.9!(bottommid)$);

      \coordinate (step1) at ($(step0)!0.25!(step4)$);
      \coordinate (step2) at ($(step0)!0.5!(step4)$);
      \coordinate (step3) at ($(step0)!0.75!(step4)$);

      \def\halfwidth{20mm}

      % Frame 2
      \node<2->[conf, colSrc, xshift=-\halfwidth] (s0) at (step0) {$s_0$};
      \node<2->[conf, colSrc] (s1) at (s0 |- step2) {$s_1$};
      \node<2->[conf, colSrc] (s2) at (s0 |- step3) {$s_2$};
      \node<2->[conf, colSrc] (s3) at (s0 |- step4) {$s_3$};

      \node<2->[conf, colTgt, xshift=\halfwidth] (t0) at (step0) {$t_0$};
      \node<2->[conf, colTgt] (t1) at (t0 |- step1) {$t_1$};
      \node<2->[conf, colTgt] (t2) at (t0 |- step2) {$t_2$};
      \node<2->[conf, colTgt] (t3) at (t0 |- step4) {$t_3$};

      \draw<2->[step arrow, colSrc, aspect on=<7->] (s0) -- node (syns1) {} (s1);
      \draw<2->[stut arrow, colSrc, aspect on=<7->] (s1) -- node (begs) {} (s2);
      \draw<2->[step arrow, colSrc, aspect on=<7->] (s2) -- node (syns2) {} (s3);

      \draw<2->[stut arrow, colTgt, aspect on=<7->] (t0) -- node (begt) {} (t1);
      \draw<2->[step arrow, colTgt, aspect on=<7->] (t1) -- node (synt1) {} (t2);
      \draw<2->[step arrow, colTgt, aspect on=<7->] (t2) -- node (synt2) {} (t3);

      % Frame 3
      \node<3-5>[legend] (begt-leg) at ({$(topleft)!0.9!(topright)$} |- begt) {Stuttering};
      \draw<3-5>[legend arr] (begt-leg) -- (begt);

      \node<3-5>[legend] (begs-leg) at ({$(topleft)!0.1!(topright)$} |- begs) {Stuttering};
      \draw<3-5>[legend arr] (begs-leg) -- (begs);

      % Frame 4
      \node<4-5>[legend] (syn1-leg) at ($(syns1)!0.5!(synt1)$) {Synchronous Step};
      \draw<4-5>[legend arr] (syn1-leg) -- (syns1);
      \draw<4-5>[legend arr] (syn1-leg) -- (synt1);

      \node<4-5>[legend] (syn2-leg) at ($(syns2)!0.5!(synt2)$) {Synchronous Step};
      \draw<4-5>[legend arr] (syn2-leg) -- (syns2);
      \draw<4-5>[legend arr] (syn2-leg) -- (synt2);

      % Frame 5
      \draw<5, 8->[rel] (s0) -- node [legend] {Related} (t0);
      \draw<5, 8->[rel] (s1) -- node [legend] {Related} (t2);
      \draw<5, 8->[rel] (s3) -- node [legend] {Related} (t3);

      % Frame 6
      % Clear the labels

      % Frame 7
      % The steps change to dash dot

      % Frame 8
      \node<8->[fuel, left=0mm of syns1] {1};
      \node<8->[fuel, left=0mm of begs] {1};
      \node<8->[fuel, left=0mm of syns2] {2};

      \node<8->[fuel, right=0mm of begt] {1};
      \node<8->[fuel, right=0mm of synt1] {2};
      \node<8->[fuel, right=0mm of synt2] {1};

      % Frame 9
      \node<9->[idx legend] (idx) at (s1 -| {$(topleft)!0.1!(topright)$}) {Index\\ Reset};
      \draw<9->[legend arr] (idx) to[out=90, in=180] (s0);
      \draw<9->[legend arr] (idx) -- (s1);
      \draw<9->[legend arr] (idx) to[out=-90, in=180] (s3);

    \end{tikzpicture}
  \end{center}
\end{frame}

\begin{frame}[label=theory]
  \frametitle{Theoretical Guarantees}

  \pause

  \textcolor{grayText}{Soundness:}
  \begin{equation*}
    \textcolor{grayText}{\forall i\, j,\qquad }
    \fsimrel{T}{i}{j}{S} \implies \refines{T}{S}
  \end{equation*}

  \vspace{5mm}
  \pause

  \textcolor{grayText}{Equivalence:}
  \begin{equation*}
    (\textcolor{grayText}{\exists i\, j,\ } \fsimrel{T}{i}{j}{S})
    \iff \textcolor{colTgt}{T} \sim_{\text{explicit}} \textcolor{colSrc}{S}
  \end{equation*}

  \vspace{5mm}
  \pause

  \textcolor{grayText}{Index Irrelevance:}
  \begin{equation*}
    (\textcolor{grayText}{\exists i\, j,\ } \fsimrel{T}{i}{j}{S})
    \implies
    \textcolor{grayText}{\forall i\, j,\quad } \fsimrel{T}{i}{j}{S}
  \end{equation*}
\end{frame}

\def\treesibdist{12mm}
\def\treelevdist{12mm}

\begin{frame}[label=asymmetry]
  \frametitle{Asymmetric Reasoning}

  \begin{center}
    \begin{tikzpicture}[
        header/.style={
          minimum height=8mm, minimum width=40mm,
          % draw
        },
        link/.style={
          thick, ->,
        }
      ]
      \bbox{}

      \coordinate[xshift=10mm] (tblstart) at ($(topleft)!0.1!(bottomleft)$);
      \coordinate[xshift=-10mm] (tblend) at ($(topright)!0.75!(bottomright)$);

      \coordinate (mid) at ({$(tblstart)!0.2!(tblend)$} -| topmid);

      \draw<2->[gridline] (mid -| tblstart) -- (mid -| tblend);
      \draw<2->[gridline] (mid |- tblstart) -- (mid |- tblend);

      \node<2->[header, text=colTgt] (tgt-head) at ($(tblstart)!0.5!(mid)$) {Target};
      \node<2->[header, text=colSrc] (src-head) at ($(mid)!0.5!(tblend |- tblstart)$) {Source};

      \node<3->[below=1mm of tgt-head, font=\small] (tgt-nd)
      {\emoji{smiling-face-with-horns} nondeterminism};

      % Target tree
      \node<4->[below=1mm of tgt-nd, text=colTgt] (roott) {$\forall$};
      \node<4-> (t-l) at ($(roott) + (-0.5*\treesibdist, -\treelevdist)$) {};
      \node<4->[text=colTgt] (t-r) at ($(roott) + (0.5*\treesibdist, -\treelevdist)$)
      {$\forall$};
      \node<4-> (endt) at ($(t-r) + (-0.5*\treesibdist, -\treelevdist)$) {};
      \node<4-> (t-rr) at ($(t-r) + (0.5*\treesibdist, -\treelevdist)$) {};

      \draw<4->[link, colTgt] (roott) -- (t-l);
      \draw<4->[link, colTgt] (roott) -- (t-r);
      \draw<4->[link, colTgt] (t-r) -- (endt);
      \draw<4->[link, colTgt] (t-r) -- (t-rr);

      \node<5->[below=1mm of src-head, font=\small] (src-nd)
      {\emoji{smiling-face-with-halo} nondeterminism};

      % Source Tree
      \node<6->[below=1mm of src-nd, text=colSrc] (roots) {$\exists$};
      \node<6-> (s-l) at ($(roots) + (-0.5*\treesibdist, -\treelevdist)$) {};
      \node<6->[text=colSrc] (s-r) at ($(roots) + (0.5*\treesibdist, -\treelevdist)$)
      {$\exists$};
      \node<6-> (ends) at ($(s-r) + (-0.5*\treesibdist, -\treelevdist)$) {};
      \node<6-> (s-rr) at ($(s-r) + (0.5*\treesibdist, -\treelevdist)$) {};

      \draw<6->[link, grayDraw]   (roots) -- (s-l);
      \draw<6->[link, colSrc] (roots) -- (s-r);
      \draw<6->[link, colSrc] (s-r)   -- (ends);
      \draw<6->[link, grayDraw]   (s-r)   -- (s-rr);


      \node<7-> at ($(tblend -| bottommid)!0.5!(bottommid)$) {
        $\text{\lightning} \in \textcolor{colSrc}{S}
        \implies
        \textcolor{colTgt}{T} \sim_{\text{free}} \textcolor{colSrc}{S}
        $
      };
    \end{tikzpicture}
  \end{center}
\end{frame}

\section{Separation Logic}

\begin{frame}[plain, noframenumbering, label=intro-logic]

  \begin{tikzpicture}[remember picture, overlay]
    \bbox[b]

    \coordinate (sep) at ($(topmid)!0.6!(bottommid)$);
    \coordinate (sepleft) at (topleft |- sep);
    \coordinate (sepright) at (bottomright |- sep);

    \coordinate (l2) at ($(sep) - (0, \gaplength + \cellheight/2)$);
    \coordinate (l3) at ($(sep) + (0, \gaplength + \cellheight/2)$);

    \coordinate (l1) at ($(l2) - (0, \cellheight + \gaplength)$);
    \coordinate (l4) at ($(l3) + (0, \cellheight + \gaplength)$);

    \node[box, node-gray, minimum width=\cellwidth] at (l1)
    {
      \textbf{Backward Simulation} \\
      {
        \scriptsize\itshape%
        inspired by ``Stuttering For Free''
      }
    };

    \node[box, node-teal, minimum width=\cellwidth] at (l2)
    {
      \textbf{Relational Separation Logic} \\
      {
        \scriptsize\itshape%
        inspired by ``Simuliris''
      }
    };

    \draw[divider] (sepleft) -- (sepright);
    \node[anchor=north west, scope-tag] at (sepleft) {Language\\Independent};
    \node[anchor=south west, scope-tag] at (sepleft) {Language\\Dependent};

    \node[box, node-gray, minimum width=\cellwidth] (rtl) at (l3)
    {
      \textbf{Low-level language} \\
      {
        \scriptsize\itshape%
        inspired by ``CompCert''
      }
    };

    \node[box, node-gray, anchor=west,
    minimum width={\cellwidth*\cellsplit - \gaplength/2}] at (l4 -| rtl.west)
    {
      \textbf{Transformations} \\
      {
        \scriptsize\itshape%
        of concrete programs
      }
    };
 
    \node[box, node-gray, anchor=east,
    minimum width={\cellwidth*(1-\cellsplit) - \gaplength/2}] at (l4 -| rtl.east)
    {
      \textbf{Optimizations} \\
      {
        \scriptsize\itshape%
        of generic programs
      }
    };
  \end{tikzpicture}
\end{frame}


\begin{frame}[label=hoare]
  \frametitle{Relational Hoare Quadruples}

  \begin{center}
    \begin{tikzpicture}
      \bbox{}

      \node<2->[font=\huge] at ($(topmid)!0.25!(bottommid)$)
      {
        $\{P\}\; \textcolor{colTgt}{f_t} \lesssim \textcolor{colSrc}{f_s} \;\{ Q\}$
      };

      \node<3->[font=\large] at ($(topmid)!0.4!(bottommid)$)
      {
        ${} \triangleq
        P \wand
        \textsf{FreeSim}\left(
          \textcolor{colTgt}{f_t},
          \textcolor{colSrc}{f_s},
          Q
        \right)
        $
      };

      \node<4->[font=\small]
      at ({$(topmid)!0.55!(bottommid)$} -| {$(topleft)!0.15!(topright)$})
      {If $P\left(\textcolor{colTgt}{\vec{v_t}},
          \textcolor{colSrc}{\vec{v_s}}\right)$: };

      \node<5->[font=\small] at ($(topmid)!0.64!(bottommid)$)
      {$\text{\lightning} \in \textcolor{colSrc}{f_s}$};

      \node<6->[font=\itshape\small] at ($(topmid)!0.72!(bottommid)$) {or};

      \node<6->[font=\small] at ($(topmid)!0.85!(bottommid)$)
      {
        $
        \left\{
          \begin{matrix}
            \mathtt{Term}(\textcolor{colTgt}{v_t}, \textcolor{colTgt}{m_t})
            \in \textcolor{colTgt}{f_t}
            \implies
            \mathtt{Term}(\textcolor{colSrc}{v_s}, \textcolor{colSrc}{m_s})
            \in \textcolor{colSrc}{f_s}
            \text{ with }
            Q\left(\textcolor{colTgt}{v_t}, \textcolor{colSrc}{v_s},
            \textcolor{colTgt}{m_t}, \textcolor{colSrc}{m_s}\right)
            \\[.5em]
            \mathtt{Div} \in \textcolor{colTgt}{f_t}
            \implies
            \mathtt{Div} \in \textcolor{colSrc}{f_s}
          \end{matrix}
          \right.
        $
      };

    \end{tikzpicture}
  \end{center}
\end{frame}


\begin{frame}[label=logic]
  \frametitle{Relational Separation Logic}

  \begin{center}
    \begin{tikzpicture}[
        memline/.style={
          draw=blackDraw, thick
        },
        lowmeml/.style={
          memline, dashed, draw=blackDraw
        },
        uppmeml/.style={
          lowmeml, dash phase=-1pt
        },
        title/.style={
          font=\small, text=grayText
        },
        cell/.style={
          font=\small,
          text width=14mm,
          align=center,
        },
        loc/.style={
          font=\scriptsize,
          % draw,
        },
        leg/.style={
          % draw
        },
        braceleg/.style={
          decorate, thick, decoration={brace, amplitude=10pt},
        },
        bgfill/.style={
          opacity=0.2,
        }
      ]
      \bbox{}

      \coordinate (header) at ($(topmid)!0.2!(bottommid)$);
      \coordinate (footer) at ($(topmid)!0.95!(bottommid)$);

      \coordinate (l1) at ($(header)!0.1!(footer)$);
      \coordinate (l2) at ($(header)!0.3!(footer)$);
      \coordinate (l3) at ($(header)!0.5!(footer)$);
      \coordinate (l4) at ($(header)!0.7!(footer)$);
      \coordinate (l5) at ($(header)!0.9!(footer)$);

      \coordinate (tgt-l) at ($(topleft)!0.75!(topright)$);
      \coordinate (tgt-r) at ($(topleft)!0.9!(topright)$);
      \coordinate (tgt-m) at ($(tgt-l)!0.5!(tgt-r)$);
      \coordinate (tgt-leg) at ($(topmid)!0.5!(tgt-l)$);

      \coordinate (src-l) at ($(topleft)!0.1!(topright)$);
      \coordinate (src-r) at ($(topleft)!0.25!(topright)$);
      \coordinate (src-m) at ($(src-l)!0.5!(src-r)$);
      \coordinate (src-leg) at ($(src-r)!0.5!(topmid)$);

      \draw[memline] (l1 -| tgt-l) -- (l1 -| tgt-r);
      \draw[memline] (l2 -| tgt-l) -- (l2 -| tgt-r);
      \draw[memline] (l3 -| tgt-l) -- (l3 -| tgt-r);
      \draw[memline] (l4 -| tgt-l) -- (l4 -| tgt-r);
      \draw[memline] (l5 -| tgt-l) -- (l5 -| tgt-r);

      \draw[uppmeml] (header -| tgt-l) -- (l1 -| tgt-l);
      \draw[memline] (l1 -| tgt-l) -- (l5 -| tgt-l);
      \draw[lowmeml] (l5 -| tgt-l) -- (footer -| tgt-l);

      \draw[uppmeml] (header -| tgt-r) -- (l1 -| tgt-r);
      \draw[memline] (l1 -| tgt-r) -- (l5 -| tgt-r);
      \draw[lowmeml] (l5 -| tgt-r) -- (footer -| tgt-r);

      \node<-2>[title] at ($(tgt-m)!0.5!(tgt-m |- header)$) {Heap};

      \node<2> at (l3 -| src-r)
      {$\textcolor{tealText}{P} \sep \textcolor{magentaText}{Q}$};
      \node<2> at (l3) {$\implies$};

      \begin{scope}[on background layer]
        \fill<2>[bgfill, tealDraw] (l1 -| tgt-l) rectangle (l3 -| tgt-r);
        \fill<2>[bgfill, magentaDraw] (l3 -| tgt-l) rectangle (l5 -| tgt-r);
      \end{scope}

      \draw<2>[braceleg, tealText] (l3 -| tgt-l) -- (l1 -| tgt-l)
      node[midway, font=\small, left=1em] {$P$};

      \draw<2>[braceleg, magentaText] (l5 -| tgt-l) -- (l3 -| tgt-l)
      node[midway, font=\small, left=1em] {$Q$};

      \draw<3->[memline] (l1 -| src-l) -- (l1 -| src-r);
      \draw<3->[memline] (l2 -| src-l) -- (l2 -| src-r);
      \draw<3->[memline] (l3 -| src-l) -- (l3 -| src-r);
      \draw<3->[memline] (l4 -| src-l) -- (l4 -| src-r);
      \draw<3->[memline] (l5 -| src-l) -- (l5 -| src-r);

      \draw<3->[uppmeml] (header -| src-l) -- (l1 -| src-l);
      \draw<3->[memline] (l1 -| src-l) -- (l5 -| src-l);
      \draw<3->[lowmeml] (l5 -| src-l) -- (footer -| src-l);

      \draw<3->[uppmeml] (header -| src-r) -- (l1 -| src-r);
      \draw<3->[memline] (l1 -| src-r) -- (l5 -| src-r);
      \draw<3->[lowmeml] (l5 -| src-r) -- (footer -| src-r);

      \node<3->[title, colTgt] at ($(tgt-m)!0.5!(tgt-m |- header)$) {Target Heap};
      \node<3->[title, colSrc] at ($(src-m)!0.5!(src-m |- header)$) {Source Heap};

      \node<4> at (l3) {$\textcolor{tealText}{P} \sep \textcolor{magentaText}{Q}$};

      \begin{scope}[on background layer]
        \fill<4>[bgfill, tealDraw] (l1 -| src-l) rectangle (l3 -| src-r);
        \fill<4>[bgfill, tealDraw] (l2 -| tgt-l) rectangle (l5 -| tgt-r);

        \fill<4>[bgfill, magentaDraw] (l3 -| src-l) rectangle (l5 -| src-r);
        \fill<4>[bgfill, magentaDraw] (l1 -| tgt-l) rectangle (l2 -| tgt-r);
      \end{scope}

      \draw<4>[braceleg, tealText] (l1 -| src-r) -- (l3 -| src-r)
      node[midway, font=\small, right=1em] {$P_{\text{src}}$};

      \draw<4>[braceleg, magentaText] (l3 -| src-r) -- (l5 -| src-r)
      node[midway, font=\small, right=1em] {$Q_{\text{src}}$};

      \draw<4>[braceleg, magentaText] (l2 -| tgt-l) -- (l1 -| tgt-l)
      node[midway, font=\small, left=1em] {$Q_{\text{tgt}}$};

      \draw<4>[braceleg, tealText] (l5 -| tgt-l) -- (l2 -| tgt-l)
      node[midway, font=\small, left=1em] {$P_{\text{tgt}}$};

      \node<5->[cell] at ({$(l2)!0.5!(l3)$} -| tgt-m) {Alloc($v'$)};
      \node<5->[loc, anchor=west] at ({$(l2)!0.5!(l3)$} -| tgt-r) {${}:l_2$};

      \begin{scope}[on background layer]
        \fill<5->[bgfill, grayDraw] (l2 -| tgt-l) rectangle (l3 -| tgt-r);
      \end{scope}

      \node<6->[leg, colTgt] at ({$(l2)!0.5!(l3)$} -| tgt-leg) {$\ptrt{l_2}{v'}$};

      \node<7->[cell] at ({$(l4)!0.5!(l5)$} -| tgt-m) {Freed};
      \node<7->[loc, anchor=west] at ({$(l4)!0.5!(l5)$} -| tgt-r) {${}:l_4$};

      \begin{scope}[on background layer]
        \fill<7->[bgfill, grayDraw] (l4 -| tgt-l) rectangle (l5 -| tgt-r);
      \end{scope}

      \node<8->[leg, colTgt] at ({$(l4)!0.5!(l5)$} -| tgt-leg) {$\freet{l_4}$};

      \node<9->[cell] at ({$(l1)!0.5!(l2)$} -| src-m) {Freed};
      \node<9->[cell] at ({$(l3)!0.5!(l4)$} -| src-m) {Alloc($v$)};

      \begin{scope}[on background layer]
        \fill<9->[bgfill, grayDraw] (l1 -| src-l) rectangle (l2 -| src-r);
        \fill<9->[bgfill, grayDraw] (l3 -| src-l) rectangle (l4 -| src-r);
      \end{scope}

      \node<9->[loc, anchor=east] at ({$(l1)!0.5!(l2)$} -| src-l) {$l_1:{}$};
      \node<9->[loc, anchor=east] at ({$(l3)!0.5!(l4)$} -| src-l) {$l_3:{}$};

      \node<9->[leg, colSrc] at ({$(l1)!0.5!(l2)$} -| src-leg) {$\frees{l_1}$};
      \node<9->[leg, colSrc] at ({$(l3)!0.5!(l4)$} -| src-leg) {$\ptrs{l_3}{v}$};

    \end{tikzpicture}
  \end{center}
\end{frame}


\begin{frame}[label=values]
  \frametitle{Value Equivalence}

  \begin{center}
    \begin{tikzpicture}[
        memline/.style={
          draw=blackDraw, thick
        },
        lowmeml/.style={
          memline, dashed, draw=blackDraw
        },
        uppmeml/.style={
          lowmeml, dash phase=-1pt
        },
        title/.style={
          font=\small, text=grayText
        },
        cell/.style={
          font=\small,
          text width=14mm,
          align=center,
        },
        bgfill/.style={
          opacity=0.2,
        },
        rel/.style={
          <->,
          line width=1pt,
          draw=violetDraw,
        },
        relbadge/.style={
          font=\small,
          text=violetText,
        },
        def/.style={
          font=\LARGE,
        },
        impl/.style={
          font=\normalsize,
        }
      ]
      \bbox{}

      \coordinate (header) at ($(topmid)!0.2!(bottommid)$);
      \coordinate (footer) at ($(topmid)!0.95!(bottommid)$);

      \coordinate (l1) at ($(header)!0.1!(footer)$);
      \coordinate (l2) at ($(header)!0.3!(footer)$);
      \coordinate (l3) at ($(header)!0.5!(footer)$);
      \coordinate (l4) at ($(header)!0.7!(footer)$);
      \coordinate (l5) at ($(header)!0.9!(footer)$);

      \coordinate (tgt-l) at ($(topleft)!0.75!(topright)$);
      \coordinate (tgt-r) at ($(topleft)!0.9!(topright)$);
      \coordinate (tgt-m) at ($(tgt-l)!0.5!(tgt-r)$);
      \coordinate (tgt-leg) at ($(tgt-l)!0.3!(topmid)$);

      \coordinate (src-l) at ($(topleft)!0.1!(topright)$);
      \coordinate (src-r) at ($(topleft)!0.25!(topright)$);
      \coordinate (src-m) at ($(src-l)!0.5!(src-r)$);
      \coordinate (src-leg) at ($(src-r)!0.3!(topmid)$);

      \begin{scope}[on background layer]
        \fill<-2>[bgfill, orangeText] (l1 -| src-l) rectangle (l2 -| src-r);
        \fill<-2>[bgfill, orangeText] (l3 -| src-l) rectangle (l5 -| src-r);

        \fill<-2>[bgfill, blueText] (l1 -| tgt-l) rectangle (l4 -| tgt-r);
      \end{scope}

      \draw<-2>[memline] (l1 -| tgt-l) -- (l1 -| tgt-r);
      \draw<-2>[memline] (l2 -| tgt-l) -- (l2 -| tgt-r);
      \draw<-2>[memline] (l3 -| tgt-l) -- (l3 -| tgt-r);
      \draw<-2>[memline] (l4 -| tgt-l) -- (l4 -| tgt-r);
      \draw<-2>[memline] (l5 -| tgt-l) -- (l5 -| tgt-r);

      \draw<-2>[uppmeml] (header -| tgt-l) -- (l1 -| tgt-l);
      \draw<-2>[memline] (l1 -| tgt-l) -- (l5 -| tgt-l);
      \draw<-2>[lowmeml] (l5 -| tgt-l) -- (footer -| tgt-l);

      \draw<-2>[uppmeml] (header -| tgt-r) -- (l1 -| tgt-r);
      \draw<-2>[memline] (l1 -| tgt-r) -- (l5 -| tgt-r);
      \draw<-2>[lowmeml] (l5 -| tgt-r) -- (footer -| tgt-r);

      \draw<-2>[memline] (l1 -| src-l) -- (l1 -| src-r);
      \draw<-2>[memline] (l2 -| src-l) -- (l2 -| src-r);
      \draw<-2>[memline] (l3 -| src-l) -- (l3 -| src-r);
      \draw<-2>[memline] (l4 -| src-l) -- (l4 -| src-r);
      \draw<-2>[memline] (l5 -| src-l) -- (l5 -| src-r);

      \draw<-2>[uppmeml] (header -| src-l) -- (l1 -| src-l);
      \draw<-2>[memline] (l1 -| src-l) -- (l5 -| src-l);
      \draw<-2>[lowmeml] (l5 -| src-l) -- (footer -| src-l);

      \draw<-2>[uppmeml] (header -| src-r) -- (l1 -| src-r);
      \draw<-2>[memline] (l1 -| src-r) -- (l5 -| src-r);
      \draw<-2>[lowmeml] (l5 -| src-r) -- (footer -| src-r);

      \node<-2>[title, colTgt] at ($(tgt-m)!0.5!(tgt-m |- header)$) {Target Heap};
      \node<-2>[title, colSrc] at ($(src-m)!0.5!(src-m |- header)$) {Source Heap};

      \node<-2>[cell] at ({$(l1)!0.5!(l2)$} -| tgt-m) {$v_1$};
      \node<-2>[cell] at ({$(l2)!0.5!(l3)$} -| tgt-m) {$v_3$};
      \node<-2>[cell] at ({$(l3)!0.5!(l4)$} -| tgt-m) {$v_4$};

      \node<-2>[cell] at ({$(l1)!0.5!(l2)$} -| src-m) {$v_2$};
      \node<-2>[cell] at ({$(l3)!0.5!(l4)$} -| src-m) {$v_5$};
      \node<-2>[cell] at ({$(l4)!0.5!(l5)$} -| src-m) {$v_6$};

      \draw<2>[rel]
      (src-leg |- {$(l1)!0.5!(l2)$}) -- (tgt-leg |- {$(l1)!0.5!(l2)$});

      \draw<2>[rel]
      (src-leg |- {$(l3)!0.5!(l4)$}) to[out=10, in=190] (tgt-leg |- {$(l2)!0.5!(l3)$});

      \draw<2>[rel]
      (src-leg |- {$(l4)!0.5!(l5)$}) to[out=10, in=190] (tgt-leg |- {$(l3)!0.5!(l4)$});

      \node<2>[relbadge] at ($(header)!0.5!(l1)$) {Partial bijection $I$};


      \node<3->[def] at (header)
      {
        $
        \sameval{\textcolor{violetText}{I}}
        {\textcolor{colTgt}{v_t}}
        {\textcolor{colSrc}{v_s}}
        $
      };

      \node<4->[impl] at (l2) {$\implies$};

      \node<4->[impl] at (l2 -| src-leg)
      {
        $\textcolor{colTgt}{i_t}=\textcolor{colSrc}{i_s}$
      };

      \node<4->[impl] at (l2 -| tgt-l)
      {
        $
        \sameval{\textcolor{violetText}{I}}
        {\VInt{\textcolor{colTgt}{i_t}}}
        {\VInt{\textcolor{colSrc}{i_s}}}
        $
      };

      \node<5->[impl] at (l3) {$\implies$};

      \node<5->[impl] at (l3 -| src-leg)
      {
        $\textcolor{colTgt}{b_t}=\textcolor{colSrc}{b_s}$
      };

      \node<5->[impl] at (l3 -| tgt-l)
      {
        $
        \sameval{\textcolor{violetText}{I}}
        {\VBool{\textcolor{colTgt}{b_t}}}
        {\VBool{\textcolor{colSrc}{b_s}}}
        $
      };

      \node<6->[impl] at (l4) {$\implies$};

      \node<6->[impl] at (l4 -| src-leg)
      {
        $\left(\textcolor{colTgt}{l_t}, \textcolor{colSrc}{l_s}\right)
        \in\textcolor{violetText}{I}$
      };

      \node<6->[impl] at (l4 -| tgt-l)
      {
        $
        \sameval{\textcolor{violetText}{I}}
        {\VPtr{\textcolor{colTgt}{l_t}}}
        {\VPtr{\textcolor{colSrc}{l_s}}}
        $
      };

    \end{tikzpicture}
  \end{center}
\end{frame}

\begin{frame}[label=bijection]
  \frametitle{Memory Bijection}

  \begin{center}
    \begin{tikzpicture}[
        memline/.style={
          draw=blackDraw, thick
        },
        lowmeml/.style={
          memline, dashed, draw=blackDraw
        },
        uppmeml/.style={
          lowmeml, dash phase=-1pt
        },
        title/.style={
          font=\small, text=grayText
        },
        cell/.style={
          font=\small,
          text width=14mm,
          align=center,
        },
        bgfill/.style={
          opacity=0.2,
        },
        remember picture
      ]
      \bbox{}

      \coordinate (header) at ($(topmid)!0.2!(bottommid)$);
      \coordinate (footer) at ($(topmid)!0.95!(bottommid)$);

      \coordinate (l1) at ($(header)!0.1!(footer)$);
      \coordinate (l2) at ($(header)!0.3!(footer)$);
      \coordinate (l3) at ($(header)!0.5!(footer)$);
      \coordinate (l4) at ($(header)!0.7!(footer)$);
      \coordinate (l5) at ($(header)!0.9!(footer)$);

      \coordinate (tgt-l) at ($(topleft)!0.75!(topright)$);
      \coordinate (tgt-r) at ($(topleft)!0.9!(topright)$);
      \coordinate (tgt-m) at ($(tgt-l)!0.5!(tgt-r)$);
      \coordinate (tgt-leg) at ($(tgt-l)!0.3!(topmid)$);

      \coordinate (src-l) at ($(topleft)!0.1!(topright)$);
      \coordinate (src-r) at ($(topleft)!0.25!(topright)$);
      \coordinate (src-m) at ($(src-l)!0.5!(src-r)$);
      \coordinate (src-leg) at ($(src-r)!0.3!(topmid)$);

      \draw[memline] (l1 -| tgt-l) -- (l1 -| tgt-r);
      \draw[memline] (l2 -| tgt-l) -- (l2 -| tgt-r);
      \draw[memline] (l3 -| tgt-l) -- (l3 -| tgt-r);
      \draw[memline] (l4 -| tgt-l) -- (l4 -| tgt-r);
      \draw[memline] (l5 -| tgt-l) -- (l5 -| tgt-r);

      \draw[uppmeml] (header -| tgt-l) -- (l1 -| tgt-l);
      \draw[memline] (l1 -| tgt-l) -- (l5 -| tgt-l);
      \draw[lowmeml] (l5 -| tgt-l) -- (footer -| tgt-l);

      \draw[uppmeml] (header -| tgt-r) -- (l1 -| tgt-r);
      \draw[memline] (l1 -| tgt-r) -- (l5 -| tgt-r);
      \draw[lowmeml] (l5 -| tgt-r) -- (footer -| tgt-r);

      \draw[memline] (l1 -| src-l) -- (l1 -| src-r);
      \draw[memline] (l2 -| src-l) -- (l2 -| src-r);
      \draw[memline] (l3 -| src-l) -- (l3 -| src-r);
      \draw[memline] (l4 -| src-l) -- (l4 -| src-r);
      \draw[memline] (l5 -| src-l) -- (l5 -| src-r);

      \draw[uppmeml] (header -| src-l) -- (l1 -| src-l);
      \draw[memline] (l1 -| src-l) -- (l5 -| src-l);
      \draw[lowmeml] (l5 -| src-l) -- (footer -| src-l);

      \draw[uppmeml] (header -| src-r) -- (l1 -| src-r);
      \draw[memline] (l1 -| src-r) -- (l5 -| src-r);
      \draw[lowmeml] (l5 -| src-r) -- (footer -| src-r);

      \node[title, colTgt] at ($(tgt-m)!0.5!(tgt-m |- header)$) {Target Heap};
      \node[title, colSrc] at ($(src-m)!0.5!(src-m |- header)$) {Source Heap};

      \begin{scope}[on background layer]
        \fill<-4>[bgfill, violetDraw] (l1 -| src-l) rectangle (l2 -| src-r);
        \fill<-4>[bgfill, violetDraw] (l3 -| src-l) rectangle (l5 -| src-r);
        \fill<-4>[bgfill, violetDraw] (l1 -| tgt-l) rectangle (l4 -| tgt-r);
      \end{scope}

      \node[font=\Large] at (l1)
      {$\textsf{membij}_{\tikzmarknode[tealText]{exn}{\visible<4->{\mathbf{E}}}}
        (\textcolor{violetText}{\mathbf{I}})$};

      \node<2-3> at (l2)
      {
        $
        (\textcolor{colTgt}{l_t}, \textcolor{colSrc}{l_s})
        \in \textcolor{violetText}{I}
        $
      };
      \node<2-3> at ($(l2)!0.5!(l3)$) {$\Downarrow$};
      \node<2-3> at (l3) {both alive with $\sameval{\textcolor{violetText}{I}}{}{}$};
      \node<3> at ($(l3)!0.5!(l4)$) {or};
      \node<3> at (l4) {both freed};

      \node<4->[tealText, font=\small] (exn-leg) at (exn |- {$(l2)!0.5!(l3)$}) {Exceptions};
      \draw<4->[->, shorten >=1ex] (exn-leg) -- (exn);

      \node<5-> at (l4) {$\textcolor{tealText}{E} \subseteq \textcolor{violetText}{I}$};

      \begin{scope}[on background layer]
        \fill<5->[bgfill, tealDraw] (l1 -| src-l) rectangle (l2 -| src-r);
        \fill<5->[bgfill, violetDraw] (l3 -| src-l) rectangle (l5 -| src-r);
        \fill<5->[bgfill, violetDraw] (l1 -| tgt-l) rectangle (l2 -| tgt-r);
        \fill<5->[bgfill, tealDraw] (l2 -| tgt-l) rectangle (l3 -| tgt-r);
        \fill<5->[bgfill, violetDraw] (l3 -| tgt-l) rectangle (l4 -| tgt-r);
      \end{scope}

    \end{tikzpicture}
  \end{center}
\end{frame}

\section{\muRTL{}}

\begin{frame}[plain, noframenumbering, label=intro-lang]

  \begin{tikzpicture}[remember picture, overlay]
    \bbox[b]

    \coordinate (sep) at ($(topmid)!0.6!(bottommid)$);
    \coordinate (sepleft) at (topleft |- sep);
    \coordinate (sepright) at (bottomright |- sep);

    \coordinate (l2) at ($(sep) - (0, \gaplength + \cellheight/2)$);
    \coordinate (l3) at ($(sep) + (0, \gaplength + \cellheight/2)$);

    \coordinate (l1) at ($(l2) - (0, \cellheight + \gaplength)$);
    \coordinate (l4) at ($(l3) + (0, \cellheight + \gaplength)$);

    \node[box, node-gray, minimum width=\cellwidth] at (l1)
    {
      \textbf{Backward Simulation} \\
      {
        \scriptsize\itshape%
        inspired by ``Stuttering For Free''
      }
    };

    \node[box, node-gray, minimum width=\cellwidth] at (l2)
    {
      \textbf{Relational Separation Logic} \\
      {
        \scriptsize\itshape%
        inspired by ``Simuliris''
      }
    };

    \draw[divider] (sepleft) -- (sepright);
    \node[anchor=north west, scope-tag] at (sepleft) {Language\\Independent};
    \node[anchor=south west, scope-tag] at (sepleft) {Language\\Dependent};

    \node[box, node-blue, minimum width=\cellwidth] (rtl) at (l3)
    {
      \textbf{Low-level language} \\
      {
        \scriptsize\itshape%
        inspired by ``CompCert''
      }
    };

    \node[box, node-gray, anchor=west,
    minimum width={\cellwidth*\cellsplit - \gaplength/2}] at (l4 -| rtl.west)
    {
      \textbf{Transformations} \\
      {
        \scriptsize\itshape%
        of concrete programs
      }
    };
 
    \node[box, node-gray, anchor=east,
    minimum width={\cellwidth*(1-\cellsplit) - \gaplength/2}] at (l4 -| rtl.east)
    {
      \textbf{Optimizations} \\
      {
        \scriptsize\itshape%
        of generic programs
      }
    };
  \end{tikzpicture}
\end{frame}

\begin{frame}[label=murtl]
  \frametitle{\muRTL{}}

  \begin{center}
    \begin{tikzpicture}[
      instr/.style={
          node-instr,
          semithick,
          font=\small,
          minimum height=6.5mm,
          minimum width=28mm,
          rounded corners=4pt,
          align=center
        },
        goto/.style={
          ->,
          semithick,
          draw=blackDraw,
          shorten >=1.5pt,
          shorten <=1.5pt
        },
        pc/.style={
          anchor=east,
          font=\footnotesize\ttfamily,
          text=grayDraw
        },
        regtitle/.style={
          font=\footnotesize,
          text=tealText
        },
        regslot/.style={
          node-teal,
          text=blackText,
          semithick,
          font=\small,
          minimum width=10mm,
          minimum height=5mm,
          rounded corners=3pt,
          align=center
        },
        legend/.style={
          font=\small,
          text=blackText
        }
      ]

      \bbox{}

      \coordinate (instr-col) at ($(topleft)!0.35!(topright)$);
      \coordinate (regs-col) at ($(topleft)!0.8!(topright)$);

      \node<2->[instr] (c1) at ({$(topmid)!0.2!(bottommid)$} -| instr-col)
      {$r_1 \coloneq r_2 + r_3$};

      \node<2->[instr] (c5) at ({$(topmid)!0.8!(bottommid)$} -| instr-col)
      {$\mathtt{ret}\; r_2$};

      \node<2->[instr] (c2) at ({$(c1)!0.25!(c5)$} -| instr-col)
      {$\mathtt{nop}$};

      \node<2->[instr] (c3) at ({$(c1)!0.5!(c5)$} -| instr-col)
      {$r_2 \coloneq \mathtt{load}\; r_1$};

      \node<2->[instr] (c4) at ({$(c1)!0.75!(c5)$} -| instr-col)
      {$\mathtt{test}\; r_4$};

      \node<2->[pc] at (c1.west) {0:};
      \node<2->[pc] at (c2.west) {1:};
      \node<2->[pc] at (c3.west) {2:};
      \node<2->[pc] at (c4.west) {3:};
      \node<2->[pc] at (c5.west) {4:};

      \draw<2->[goto] (c1.south) -- (c2.north);
      \draw<2->[goto] (c2.south) -- (c3.north);
      \draw<2->[goto] (c3.south) -- (c4.north);
      \draw<2->[goto]
      (c4.south) --
      node[midway, right=1pt, font=\scriptsize\sffamily, text=greenText] {True} (c5.north);

      \draw<2->[goto, rounded corners=4pt] (c4.east) -- ++(8mm, 0)
      node[pos=0.5, above=1pt, font=\scriptsize\sffamily, text=redText] {False}
      |- (c1.east);

      \node<2->[legend] at ({$(topmid)!0.9!(bottommid)$} -| instr-col) {Function body};

      \coordinate (regs) at (regs-col |- c3);

      \node<3->[regslot, above left=2pt of regs] {$r_1$};
      \node<3->[regslot, above right=2pt of regs] {$r_2$};
      \node<3->[regslot, below left=2pt of regs] {$r_3$};
      \node<3->[regslot, below right=2pt of regs]  {$r_4$};

      \node<3->[regtitle, above=6mm of regs] {Local Registers};

    \end{tikzpicture}
  \end{center}
\end{frame}


\begin{frame}[label=semantics]
  \frametitle{Operational Semantics}

    \begin{center}
      \begin{tikzpicture}[
        tuple/.style={
          font=\Large,
          text=blackText,
        },
        leg/.style={
          font=\footnotesize,
          text=grayText,
        },
        leg arr/.style={
          ->, semithick, draw=grayDraw,
        },
        state/.style={
          node-gray,
          text=blackText,
          minimum height=7.5mm,
          minimum width=26mm,
          rounded corners=4.5pt,
          font=\small\bfseries,
          thick,
        },
        state arr/.style={
          ->,
          thick,
          shorten >=1.5pt,
          shorten <=1.5pt,
          draw=grayDraw,
        },
        state leg/.style={
          font=\scriptsize,
          text=grayText,
        },
        mod label/.style={
          font=\footnotesize,
          text=grayText,
          inner sep=0pt,
        },
        mod box/.style={
          font=\small,
          text=blackText,
        },
      ]
      \bbox{}

      \node<2->[tuple] (state) at ($(topmid)!0.1!(bottommid)$) {$(\sigma, s, m)$};

      \node<3->[leg] at ({$(topmid)!0.2!(bottommid)$} -| {$(topleft)!0.25!(topright)$})
        (cs-leg) {Call Stack};
      \node<3->[leg] at ({$(topmid)!0.25!(bottommid)$} -| topmid) (s-leg) {State};
      \node<3->[leg] at ({$(topmid)!0.2!(bottommid)$} -| {$(topleft)!0.75!(topright)$})
        (m-leg) {Memory};

      \draw<3->[leg arr] (cs-leg) -- ($(state.west)!0.5!(state.south west)$);
      \draw<3->[leg arr] (s-leg) -- (state.south);
      \draw<3->[leg arr] (m-leg) -- ($(state.east)!0.5!(state.south east)$);

      \coordinate (l1) at ($(topmid)!0.42!(bottommid)$);
      \coordinate (l3) at ($(topmid)!0.72!(bottommid)$);
      \coordinate (l2) at ($(l1)!0.5!(l3)$);

      \coordinate (c1) at ($(topleft)!0.15!(topright)$);
      \coordinate (c2) at ($(topleft)!0.5!(topright)$);
      \coordinate (c3) at ($(topleft)!0.85!(topright)$);

      \node<4->[state] (call)   at (c1 |- l1) {Call State};
      \node<5->[state] (run)    at (c2 |- l2) {Running State};
      \node<7->[state] (return) at (c3 |- l3) {Return State};

      \draw<5->[state arr]
        (call.east)
        to[out=0, in=90] node [pos=0.2, above, state leg] {start}
        ($(run.north west)!0.2!(run.north)$);

      \draw<6->[state arr]
        ($(run.north west)!0.4!(run.north east)$)
        to[out=120, in=60, looseness=5] node [above, state leg] {step}
        ($(run.north west)!0.6!(run.north east)$);

      \draw<7->[state arr]
        ($(run.south)!0.8!(run.south east)$)
        to[out=-90, in=180] node [pos=0.15, left=2pt, state leg] {end}
        (return.west);

      \draw<8->[state arr]
        (run.west)
        to[out=180, in=-90] node [pos=0.2, below, state leg] {call}
        ($(call.south east)!0.2!(call.south)$);

      \draw<8->[state arr]
        ($(return.north west)!0.2!(return.north)$)
        to[out=90, in=0] node [pos=0.15, right=2pt, state leg] {return}
        (run.east);

      \node<9->[anchor=west, mod label]
        at ({$(topmid)!0.8!(bottommid)$} -| {$(topleft)!0.05!(topright)$}) {Modularity:};

      \node<9->[mod box] at ($(topmid)!0.9!(bottommid)$)
      {
        $
        \step{([], s, m)}{([], s', m')}
        \implies
        \step{(\sigma, s, m)}{(\sigma, s', m')}
        $
      };
    \end{tikzpicture}
  \end{center}
\end{frame}

\section{Case Studies}

\begin{frame}[plain, noframenumbering, label=intro-trans]

  \begin{tikzpicture}[remember picture, overlay]
    \bbox[b]

    \coordinate (sep) at ($(topmid)!0.6!(bottommid)$);
    \coordinate (sepleft) at (topleft |- sep);
    \coordinate (sepright) at (bottomright |- sep);

    \coordinate (l2) at ($(sep) - (0, \gaplength + \cellheight/2)$);
    \coordinate (l3) at ($(sep) + (0, \gaplength + \cellheight/2)$);

    \coordinate (l1) at ($(l2) - (0, \cellheight + \gaplength)$);
    \coordinate (l4) at ($(l3) + (0, \cellheight + \gaplength)$);

    \node[box, node-gray, minimum width=\cellwidth] at (l1)
    {
      \textbf{Backward Simulation} \\
      {
        \scriptsize\itshape%
        inspired by ``Stuttering For Free''
      }
    };

    \node[box, node-gray, minimum width=\cellwidth] at (l2)
    {
      \textbf{Relational Separation Logic} \\
      {
        \scriptsize\itshape%
        inspired by ``Simuliris''
      }
    };

    \draw[divider] (sepleft) -- (sepright);
    \node[anchor=north west, scope-tag] at (sepleft) {Language\\Independent};
    \node[anchor=south west, scope-tag] at (sepleft) {Language\\Dependent};

    \node[box, node-gray, minimum width=\cellwidth] (rtl) at (l3)
    {
      \textbf{Low-level language} \\
      {
        \scriptsize\itshape%
        inspired by ``CompCert''
      }
    };

    \node[box, node-orange, anchor=west,
    minimum width={\cellwidth*\cellsplit - \gaplength/2}] at (l4 -| rtl.west)
    {
      \textbf{Transformations} \\
      {
        \scriptsize\itshape%
        of concrete programs
      }
    };
 
    \node[box, node-gray, anchor=east,
    minimum width={\cellwidth*(1-\cellsplit) - \gaplength/2}] at (l4 -| rtl.east)
    {
      \textbf{Optimizations} \\
      {
        \scriptsize\itshape%
        of generic programs
      }
    };
  \end{tikzpicture}
\end{frame}

\begin{frame}[label=zerocheck]
  \frametitle{Eliminating a Redundant Zero-Check}

  \begin{center}
    \begin{tikzpicture}[
        instr/.style={
          text=blackText,
          font=\small,
          inner sep=5pt,
        },
        pc/.style={
          font=\footnotesize\ttfamily,
          text=grayDraw,
        },
        title/.style={
          font=\small,
        },
        proof/.style={
          font=\footnotesize,
          text=violetText,
        },
        inv/.style={
          font=\small,
          text=violetText,
        },
        highlight/.style={
          opacity=0.17, fill=greenDraw
        }
      ]
      \bbox{}

      \coordinate (src-m) at ($(topleft)!0.1!(topright)$);
      \coordinate (tgt-m) at ($(topleft)!0.68!(topright)$);
      \coordinate (case-l) at ($(topleft)!0.01!(topright)$);
      \coordinate (case-r) at ($(topleft)!0.99!(topright)$);

      \coordinate (before) at ($(topmid)!0.2!(bottommid)$);
      \coordinate (code1)  at ($(topmid)!0.4!(bottommid)$);
      \coordinate (code2)  at ($(topmid)!0.7!(bottommid)$);
      \coordinate (after)  at ($(topmid)!0.9!(bottommid)$);

      \node<2->[title, colSrc, anchor=west]
        at ({$(topmid)!0.6!(before)$} -| src-m) {Source Code};

      \node<2->[pc, anchor=east] at (src-m |- before) {$1:$};
      \node<2->[pc, anchor=east] at (src-m |- code1)  {$2:$};
      \node<2->[pc, anchor=east] at (src-m |- code2)  {$3:$};
      \node<2->[pc, anchor=east] at (src-m |- after)  {$4:$};

      \node<2->[instr, anchor=west] (cs1) at (src-m |- before)
      {
        $\dots$
        \textcolor{grayDraw}{\footnotesize ${}\to 2$}
      };

      \node<2->[instr, anchor=west] (cs2) at (src-m |- code1)
      {
        $r_1 \coloneq r_n / r_d$
        \textcolor{grayDraw}{\footnotesize $ {}\to 3$}
      };

      \node<2->[instr, anchor=west] (cs3) at (src-m |- code2)
      {
        $r_2 \coloneq \mathtt{test}\; r_d$
        \textcolor{grayDraw}{\footnotesize ${}\to 4$}
      };

      \node<2->[instr, anchor=west] (cs4) at (src-m |- after) {$\dots$};

      \fill<3-5>[highlight] (cs3.north east) rectangle  (cs3.south west);

      \node<4->[pc, anchor=east] at (tgt-m |- code2)  {$3:$};
      \node<4->[instr, anchor=west] (ct3) at (tgt-m |- code2)
      {
        $r_2 \coloneq \texttt{false}$
        \textcolor{grayDraw}{\footnotesize ${}\to 4$}
      };

      \node<4-5>[font=\large, text=grayText] at (code2) {$\implies$};

      \fill<4-5>[highlight] (ct3.north east) rectangle  (ct3.south west);

      \node<5->[title, colTgt, anchor=west]
        at ({$(topmid)!0.6!(before)$} -| tgt-m) {Target Code};

      \node<5->[pc, anchor=east] at (tgt-m |- before) {$1:$};
      \node<5->[pc, anchor=east] at (tgt-m |- code1)  {$2:$};
      \node<5->[pc, anchor=east] at (tgt-m |- after)  {$4:$};

      \node<5->[instr, anchor=west] (ct1) at (tgt-m |- before)
      {
        $\dots$
        \textcolor{grayDraw}{\footnotesize ${}\to 2$}
      };

      \node<5->[instr, anchor=west] (ct2) at (tgt-m |- code1)
      {
        $r_1 \coloneq r_n / r_d$
        \textcolor{grayDraw}{\footnotesize ${}\to 3$}
      };

      \node<5->[instr, anchor=west] (ct4) at (tgt-m |- after) {$\dots$};

      \node<6->[inv] at ($(before)!0.5!(code1)$)
      {
        $
        \{
        \textsf{membij}_{\emptyset}(I) \sep \textsf{same\_regs}\left(I\right)
        \}
        $
      };

      \node<7->[inv] (rz) at ($(code1)!0.33!(code2)$) {$\{ \top \}$};
      \node<7->[proof, anchor=base west] at (rz.base -| case-l)
        {\cbullet~$r_d = 0:$};

      \node<8->[proof, anchor=east] at (rz -| case-r)
        {because $\text{\lightning} \in \textcolor{colSrc}{\text{source}}$};

      \node<9->[inv] (rnz) at ($(code1)!0.66!(code2)$)
      {
        $
        \{
        \textsf{membij}_{\emptyset}(I) \sep \textsf{same\_regs}\left(I\right)
        \sep r_d \neq 0
        \}
        $
      };
      \node<9->[proof, anchor=base west] at (rnz.base -| case-l)
        {\cbullet~$r_d \neq 0:$};

      \node<10->[inv] at ($(code2)!0.5!(after)$)
      {
        $
        \{
        \textsf{membij}_{\emptyset}(I) \sep \textsf{same\_regs}\left(I\right)
        \}
        $
      };

    \end{tikzpicture}
  \end{center}
\end{frame}

\begin{frame}[plain, noframenumbering, label=intro-optim]

  \begin{tikzpicture}[remember picture, overlay]
    \bbox[b]

    \coordinate (sep) at ($(topmid)!0.6!(bottommid)$);
    \coordinate (sepleft) at (topleft |- sep);
    \coordinate (sepright) at (bottomright |- sep);

    \coordinate (l2) at ($(sep) - (0, \gaplength + \cellheight/2)$);
    \coordinate (l3) at ($(sep) + (0, \gaplength + \cellheight/2)$);

    \coordinate (l1) at ($(l2) - (0, \cellheight + \gaplength)$);
    \coordinate (l4) at ($(l3) + (0, \cellheight + \gaplength)$);

    \node[box, node-gray, minimum width=\cellwidth] at (l1)
    {
      \textbf{Backward Simulation} \\
      {
        \scriptsize\itshape%
        inspired by ``Stuttering For Free''
      }
    };

    \node[box, node-gray, minimum width=\cellwidth] at (l2)
    {
      \textbf{Relational Separation Logic} \\
      {
        \scriptsize\itshape%
        inspired by ``Simuliris''
      }
    };

    \draw[divider] (sepleft) -- (sepright);
    \node[anchor=north west, scope-tag] at (sepleft) {Language\\Independent};
    \node[anchor=south west, scope-tag] at (sepleft) {Language\\Dependent};

    \node[box, node-gray, minimum width=\cellwidth] (rtl) at (l3)
    {
      \textbf{Low-level language} \\
      {
        \scriptsize\itshape%
        inspired by ``CompCert''
      }
    };

    \node[box, node-gray, anchor=west,
    minimum width={\cellwidth*\cellsplit - \gaplength/2}] at (l4 -| rtl.west)
    {
      \textbf{Transformations} \\
      {
        \scriptsize\itshape%
        of concrete programs
      }
    };
 
    \node[box, node-violet, anchor=east,
    minimum width={\cellwidth*(1-\cellsplit) - \gaplength/2}] at (l4 -| rtl.east)
    {
      \textbf{Optimizations} \\
      {
        \scriptsize\itshape%
        of generic programs
      }
    };
  \end{tikzpicture}
\end{frame}


\begin{frame}[label=tunneling]
  \frametitle{Short-Circuiting Jump Chains}

  \begin{center}
    \begin{tikzpicture}[
        instr/.style={
          node-instr,
          semithick,
          font=\small,
          minimum height=6mm,
          minimum width=10mm,
          rounded corners=4pt,
          align=center
        },
        goto/.style={
          ->,
          semithick,
          draw=blackDraw,
          shorten >=1.5pt,
          shorten <=1.5pt
        },
        pc/.style={
          anchor=north,
          font=\footnotesize\ttfamily,
          text=grayDraw
        },
      ]
      \bbox{}

      \coordinate (l1) at ($(topmid)!0.15!(bottommid)$);
      \coordinate (l2) at ($(topmid)!0.6!(bottommid)$);
      \coordinate (l3) at ($(topmid)!0.85!(bottommid)$);

      \coordinate (c1) at ($(topleft)!0.1!(topright)$);
      \coordinate (c6) at ($(topleft)!0.9!(topright)$);

      \coordinate (c2) at ($(c1)!0.2!(c6)$);
      \coordinate (c3) at ($(c1)!0.4!(c6)$);
      \coordinate (c4) at ($(c1)!0.6!(c6)$);
      \coordinate (c5) at ($(c1)!0.8!(c6)$);

      \node[instr] (i1) at (l1 -| c1) {$i$};
      \node[instr] (i2) at (l1 -| c2) {\texttt{nop}};
      \node[instr] (i3) at (l1 -| c3) {\texttt{nop}};
      \node[instr] (i4) at (l1 -| c4) {\texttt{nop}};
      \node[instr] (i5) at (l1 -| c5) {\texttt{nop}};
      \node[instr] (i6) at (l1 -| c6) {$i'$};

      \node[pc] at (i1.south) {1};
      \node[pc] at (i2.south) {2};
      \node[pc] at (i3.south) {3};
      \node[pc] at (i4.south) {4};
      \node[pc] at (i5.south) {5};
      \node[pc] at (i6.south) {6};

      \draw[goto] (i1) -- (i2);
      \draw[goto] (i2) -- (i3);
      \draw[goto] (i3) -- (i4);
      \draw[goto] (i4) -- (i5);
      \draw[goto] (i5) -- (i6);

      \node<2->[font=\Large] at ($(l1)!0.4!(l2)$) {$\Downarrow$};

      \node<2->[instr] (i'1) at (l2 -| c1) {$i$};
      \node<2->[instr] (i'2) at (l2 -| c2) {\texttt{nop}};
      \node<2->[instr] (i'3) at (l2 -| c3) {\texttt{nop}};
      \node<2->[instr] (i'4) at (l2 -| c4) {\texttt{nop}};
      \node<2->[instr] (i'5) at (l2 -| c5) {\texttt{nop}};
      \node<2->[instr] (i'6) at (l2 -| c6) {$i'$};

      \node<2->[pc] at (i'1.south) {1};
      \node<2->[pc] at (i'2.south) {2};
      \node<2->[pc] at (i'3.south) {3};
      \node<2->[pc] at (i'4.south) {4};
      \node<2->[pc] at (i'5.south) {5};
      \node<2->[pc] at (i'6.south) {6};

      \draw<2->[goto] (i'1) to[out=35, in=140, looseness=.6] (i'6);
      \draw<2->[goto] (i'2) to[out=35, in=140, looseness=.6] (i'6);
      \draw<2->[goto] (i'3) to[out=35, in=140, looseness=.6] (i'6);
      \draw<2->[goto] (i'4) to[out=35, in=140, looseness=.6] (i'6);
      \draw<2->[goto] (i'5) -- (i'6);

      \node<3->[font=\Large] at (l3)
      {
        $\{\sameargsf{\textcolor{violetText}{I}}\}\;
        \textcolor{colTgt}{\mathsf{tunnel}_{d}\left(f\right)}
        \lesssim
        \textcolor{colSrc}{f}
        \;\{\samerf{\textcolor{violetText}{I}}\}$
      };
    \end{tikzpicture}
  \end{center}
\end{frame}

\section{Conclusion}

\begin{frame}[label=future]
  \frametitle{What Comes Next}

  \begin{center}
    \begin{tikzpicture}[
        txt/.style={
          font=\small,
          align=center,
          text=blackText,
        },
        dot/.style={
          fill=blackDraw,
        },
        arr/.style={
          draw=blackDraw,
          thick
        },
        tick/.style={
          draw=blackDraw,
          thick,
        },
      ]
      \bbox{}

      \coordinate (start)
      at ({$(topleft)!0.05!(topright)$} |- {$(topmid)!0.2!(bottommid)$});

      \coordinate (end)
      at ({$(topleft)!0.95!(topright)$} |- {$(topmid)!0.8!(bottommid)$});

      \coordinate (mid) at ($(start)!0.5!(end)$);

      \path let
        \p{s} = (start),
        \p{e} = (end),
        \p{m} = (mid),
        \n{R} = {0.5 * (\y{s} - \y{m})}
      in
        coordinate (c1-start) at (\x{e} - \n{R}, \y{s})
        coordinate (c1-end) at (\x{e} - \n{R}, \y{m})

        coordinate (c2-start) at (\x{s} + \n{R}, \y{m})
        coordinate (c2-end) at (\x{s} + \n{R}, \y{e})

        coordinate (tick1) at ($(c2-end)!0.25!(end)$);

      \coordinate[xshift=1mm] (tick2) at (tick1);
      \coordinate (pl1) at ($(start)!0.2!(c1-start)$);
      \coordinate (pl2) at ({$(start)!0.5!(mid)$} -| end);
      \coordinate (pl3) at ($(c1-end)!0.55!(c2-start)$);
      \coordinate (pl4) at ({$(mid)!0.5!(end)$} -| start);
      \coordinate (pl5) at ($(tick2)!0.3!(end)$);
      \coordinate (pl6) at ($(tick2)!0.8!(end)$);

      \path[arr]
        let
          \p{s} = (start),
          \p{m}  = (mid),
          \n{R}  = {0.5 * (\y{s} - \y{m})}
        in
          (start) -- (c1-start)
          arc[start angle=90, end angle=-90, radius=\n{R}] --
          (c1-end) -- (c2-start)
          arc[start angle=90, end angle=270, radius=\n{R}] --
          (c2-end) -- (tick1);

      \draw[tick] ($(tick1) + (-0.15, -0.3)$) -- ($(tick1) + (0.15, 0.3)$);
      \draw[tick] ($(tick2) + (-0.15, -0.3)$) -- ($(tick2) + (0.15, 0.3)$);

      \draw[arr, ->] (tick2) -- (end);

      \fill<2->[dot] (pl1) circle (2pt);
      \node<2->[txt, above=0em of pl1] {Complex\\Optimizations};

      \fill<3->[dot] (pl2) circle (2pt);
      \node<3->[txt, left=1ex of pl2] {Multi-Language\\Passes};

      \fill<4->[dot] (pl3) circle (2pt);
      \node<4->[txt, above=0em of pl3] {Simulation\\Composition};

      \fill<5->[dot] (pl4) circle (2pt);
      \node<5->[txt, right=1ex of pl4] {CompCert\\Integration};

      \fill<6->[dot] (pl5) circle (2pt);
      \node<6->[txt, above=0em of pl5] {Concurrent\\CompCert};

      \fill<7->[dot] (pl6) circle (2pt);
      \node<7->[txt, below=0em of pl6] {Verified OCaml\\Compiler};
    \end{tikzpicture}
  \end{center}
\end{frame}

\begin{frame}[label=thanks]
  \frametitle{Thanks}

  \begin{tikzpicture}[remember picture, overlay]
    \bbox[b]

    \coordinate (sep) at ($(topmid)!0.6!(bottommid)$);
    \coordinate (sepleft) at (topleft |- sep);
    \coordinate (sepright) at (bottomright |- sep);

    \coordinate (l2) at ($(sep) - (0, \gaplength + \cellheight/2)$);
    \coordinate (l3) at ($(sep) + (0, \gaplength + \cellheight/2)$);

    \coordinate (l1) at ($(l2) - (0, \cellheight + \gaplength)$);
    \coordinate (l4) at ($(l3) + (0, \cellheight + \gaplength)$);

    \node[box, node-green, minimum width=\cellwidth] at (l1)
    {
      \textbf{Backward Simulation} \\
      {
        \scriptsize\itshape%
        inspired by ``Stuttering For Free''
      }
    };

    \node[box, node-teal, minimum width=\cellwidth] at (l2)
    {
      \textbf{Relational Separation Logic} \\
      {
        \scriptsize\itshape%
        inspired by ``Simuliris''
      }
    };

    \draw[divider] (sepleft) -- (sepright);
    \node[anchor=north west, scope-tag] at (sepleft) {Language\\Independent};
    \node[anchor=south west, scope-tag] at (sepleft) {Language\\Dependent};

    \node[box, node-blue, minimum width=\cellwidth] (rtl) at (l3)
    {
      \textbf{Low-level language} \\
      {
        \scriptsize\itshape%
        inspired by ``CompCert''
      }
    };

    \node[box, node-orange, anchor=west,
    minimum width={\cellwidth*\cellsplit - \gaplength/2}] at (l4 -| rtl.west)
    {
      \textbf{Transformations} \\
      {
        \scriptsize\itshape%
        of concrete programs
      }
    };
 
    \node[box, node-violet, anchor=east,
    minimum width={\cellwidth*(1-\cellsplit) - \gaplength/2}] at (l4 -| rtl.east)
    {
      \textbf{Optimizations} \\
      {
        \scriptsize\itshape%
        of generic programs
      }
    };
  \end{tikzpicture}
\end{frame}

\end{document}

% Local Variables:
% TeX-engine: luatex
% End:
